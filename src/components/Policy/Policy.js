import React from 'react';
import PolicyContent from './PolicyContent';

const Policy = () => {
    return (
        <div>
            <PolicyContent/>
        </div>
    );
};

export default Policy;